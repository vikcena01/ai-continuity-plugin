---
schema: 1
id: n19j9
type: next_action
title: >-
  CONFIRM the community submission actually exists before waiting on it — an
  identity gate may have blocked it
status: open
confidence: confirmed
provenance:
  origin: auto
  created: '2026-08-26T08:31:21.266Z'
supersedes:
  - n18yb
superseded_by: null
depends_on: []
tags: []
---

Resolve the ambiguity first. Anthropic's individual-author route is the Console form at platform.claude.com/plugins/submit; OpenAI's platform requires a verified publisher identity too, so the same wording can appear on either. If the gate was Anthropic's, complete the verification — it is a one-time gate on a submission that CAN be completed — then confirm a submission record exists and resubmit if not. If it was OpenAI's, ignore it: x9nq rejects that path on architecture, and identity verification there means handing over ID documents for a form that cannot be finished.

Note the catalogue check cannot distinguish these cases: it lists only APPROVED plugins, so an empty result is consistent with 'in review', 'never submitted', and 'rejected' alike. Do not read absence as progress.

Everything on this side is shipped and verified: remote main and tag v1.0.0 both at abb6230, 85 assertions across five suites, dist/ in sync, history purged, published claims free of personal infrastructure.

Still outstanding regardless:
  1. FRESH SESSION: exercise the public install path, removing the directory-source marketplace first because the name collides (a2qj); also the first chance to confirm resolve_claim is exposed.
  2. COWORK APP: answer q8w6 by installing there rather than reasoning about it.
  3. When convenient: document the claim-file format contract in the README, and add CI running npm test on push so the tests badge stops being a hand-maintained claim (rk9cg).
  4. Optional, the user's own file: a `Host github.com` block in ~/.ssh/config pinning the right key (d29ka).

Never enter payment details or identity documents on the user's behalf — those steps are always theirs.
