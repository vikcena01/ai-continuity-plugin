---
schema: 1
id: n22z8
type: next_action
title: >-
  Verify install and try Cowork; set the review baseline. MCP surface already
  verified over stdio
status: open
confidence: confirmed
provenance:
  origin: auto
  created: '2026-09-03T04:13:16.634Z'
supersedes:
  - n21lz
superseded_by: null
depends_on: []
tags: []
---

1. FRESH SESSION — exercise the public install path; remove the directory-source marketplace first, the name collides (a2qj).
2. COWORK APP — answer q8w6 by installing, not reasoning.
3. REVIEW BASELINE — 97 claims, no marker. Read `continuity list`, then `continuity review --accept`.

Already verified by probing dist/mcp.js over stdio, so do NOT redo it: the server initializes on protocol 2024-11-05 and advertises 11 tools, each with a description and input schema, resolve_claim included. `review` is deliberately NOT among them — it is a human ritual, so it stays CLI-only.

Waiting, not actionable: Claude community submission in review (the catalogue lists only APPROVED plugins, so absence means nothing); CI not scheduling (rk14so); OpenAI closed (x9nq).

Glama: listed at 25%. glama.json and a `continuity-mcp` bin are done. Creating a Glama Release is the only high-value remaining step — it alone gates three of the five failing checks. Do not chase Recent Usage or Related Servers: Glama indexes servers you connect TO, and this one is local-first (d5, d6), so in their sandbox resume_context correctly errors with 'No project selected'. Demoing it there needs create_project first.
