---
id: q7f5
type: question
title: >-
  All 61 published commits are authored continuity <continuity@local>, so
  nothing links to the GitHub account
status: resolved
confidence: confirmed
provenance:
  origin: auto
  created: '2026-08-26T06:22:04.778Z'
  updated: '2026-08-26T06:46:05.429Z'
supersedes: []
superseded_by: null
depends_on: []
tags: []
resolution: >-
  Fixed: all 77 commits rewritten to Vikash Singh <vikcena01@gmail.com> (author
  and committer), verified 0 occurrences of continuity@local anywhere in
  history, and force-pushed. Root cause also fixed in src/core/git.ts so the
  placeholder identity is only a fallback — otherwise future capture commits
  would have regressed to unattributed.
---

Every commit was written by the tool's own forced inline identity (src/core/git.ts sets user.email=continuity@local so capture works with no global git config), and the user's global config is also `continuity`. Consequence now that the repo is public: GitHub cannot associate any commit with the owner's account, so the contributor graph is empty and the history looks authorless. Fixing the existing 61 commits means ANOTHER history rewrite — new SHAs again, and the claim-body SHA references would need remapping a second time (see d27c9). Recommendation given: leave v1.0.0 as it stands and set `git config --local user.name` / `user.email` to the personal identity so future commits attribute correctly. Undecided by the user.
