---
id: n14i4
type: next_action
title: >-
  Submitted to claude-community — poll the catalog for the entry; new commits
  are fine, rewrites are not
status: superseded
confidence: confirmed
provenance:
  origin: auto
  created: '2026-08-26T07:01:16.068Z'
supersedes:
  - n13vu
superseded_by: n15z8
depends_on: []
tags: []
superseded_reason: >-
  The manifest description sync that the previous next_action listed as open is
  done and pushed (c11e818), so the remaining work is verification and waiting.
---

Status check — the authoritative signal, better than waiting for a notification:
  curl -s https://raw.githubusercontent.com/anthropics/claude-plugins-community/main/.claude-plugin/marketplace.json | grep -c '"name": "continuity"'
Returns 0 now, 1 once live. Verified 2026-08-26: catalog holds 2282 plugins, no entry named `continuity` and nothing pointing at vikcena01, as expected minutes after submitting.

Timing: the docs publish NO review SLA — do not invent one. The only hard number is that the public catalogue syncs NIGHTLY from the review pipeline, so even instant approval means up to a day before it is installable.

IMPORTANT correction to the earlier framing: pushing NEW commits is fine and expected — Anthropic's CI bumps the SHA pin automatically as commits land. Only REWRITING history breaks the pin (c6vg). So the manifest description wording can still be synced with an ordinary commit; it did not have to happen before submission.

Still open: sync .claude-plugin/plugin.json and marketplace.json to the agreed public description; restart the session and exercise the public install path (marketplace-name collision — see a2qj); check the GitHub-rendered README; and q8w6, whether Cowork is actually supported.
