---
id: n16u8
type: next_action
title: >-
  Set the GitHub repo About fields, then verify the install path from a fresh
  session
status: superseded
confidence: confirmed
provenance:
  origin: auto
  created: '2026-08-26T07:11:40.949Z'
supersedes:
  - n15z8
superseded_by: n17mv
depends_on: []
tags: []
superseded_reason: >-
  The repo About fields were the last item that could be closed from here, and
  they are now verified live via the GitHub API; what remains needs either a
  fresh session, the Cowork app, or Anthropic's review to complete.
---

DONE this turn: the GitHub-rendered README verified against the actual page HTML — <picture> with prefers-color-scheme survives (GitHub wraps it in <themed-picture>), align=left on the mark survives, and all four assets serve 200 as image/svg+xml (934B, 934B, 649B, 776B). Attribution confirmed live: the sidebar reads Contributors 1 — vikcena01 Vikash Singh. MIT licence detected, 1 tag.

USER ACTION, cannot be done from a session: the repo's About panel says 'No description, website, or topics provided'. That field is separate from the plugin manifest and is what shows in GitHub search, on the profile, and when the link is shared. It needs repo-admin write, and the only gh-authenticated account is the work one with READ — which c5hi forbids using here anyway. Suggested description: 'Durable project state across AI sessions — decisions, constraints, and rejected paths, versioned in git. Not a memory tool.' Suggested topics: claude-code, claude-plugin, mcp, project-state, ai-agents, git.

Remaining:
  1. Poll for the catalogue entry: curl -s https://raw.githubusercontent.com/anthropics/claude-plugins-community/main/.claude-plugin/marketplace.json | grep -c '"name": "continuity"' — 0 now, 1 once live. No review SLA is published; the catalogue syncs nightly.
  2. Restart the session (the MCP server here still runs a pre-fix bundle) and exercise the public install path, removing the directory-source marketplace first because of the name collision (a2qj).
  3. Answer q8w6 — whether Cowork actually works — by installing there, not by reasoning about it.
  4. Optional, the user's own file: a `Host github.com` block in ~/.ssh/config pinning the personal-account key (d29ka).

New commits stay safe; only rewriting history breaks the SHA pin (c6vg).
