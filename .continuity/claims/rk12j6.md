---
schema: 1
id: rk12j6
type: risk
title: >-
  Resume context is becoming the cost it exists to remove — 38KB injected per
  session, tiering unbuilt
status: resolved
confidence: confirmed
provenance:
  origin: auto
  created: '2026-08-26T09:43:48.487Z'
  updated: '2026-08-26T14:59:44.920Z'
supersedes: []
superseded_by: null
depends_on: []
tags: []
reason: >-
  Measured, monotonically growing, and it degrades every session before the user
  types anything. A mechanical ceiling like this outranks the open bets, because
  it will make the tool feel bad even if the thesis is proven.
resolution: >-
  Fixed in 2a7beb3: tiered projection with a measured 16KB default. 41,160 ->
  12,182 bytes on this project's 49 claims with every claim still listed;
  degradation is disclosed, never silent;
  mission/milestone/direction/frozen/conflicts are never dropped. 18 assertions
  in test/budget.sh.
---

Measured 2026-08-26: the SessionStart hook injected 38KB of additionalContext — large enough that the harness spilled it to a file rather than inline it. That is at roughly 45 live claims, and it only grows. DESIGN.md section 5 anticipates token budgeting with tiered inclusion (frozen and active claims never dropped, low-tier sections summarised, plus a truthful note about what was omitted) but none of it is built; renderResumeContext emits every live claim in full.

Treat this as higher priority than q1 or q2. Those are bets about whether the idea works; this is a ceiling that makes the tool unpleasant regardless. Cheapest first cut: cap claim bodies in the projection (title always, body only for frozen/next/conflicts), since long bodies rather than claim count are what dominate the payload.
