---
id: n15z8
type: next_action
title: >-
  Waiting on community-marketplace review — poll the catalog; verify the install
  path from a fresh session
status: superseded
confidence: confirmed
provenance:
  origin: auto
  created: '2026-08-26T07:05:03.714Z'
supersedes:
  - n14i4
superseded_by: n16u8
depends_on: []
tags: []
superseded_reason: >-
  The rendered-README check is done and passed, and the repo page surfaced a new
  gap that only the repo owner can close, so it belongs at the top of the
  remaining work.
---

Poll for the entry — the authoritative status signal, better than waiting for a notification:
  curl -s https://raw.githubusercontent.com/anthropics/claude-plugins-community/main/.claude-plugin/marketplace.json | grep -c '"name": "continuity"'
0 now, 1 once live. No review SLA is published — do not invent one; the only hard number is that the public catalogue syncs nightly, so even instant approval means up to a day before it is installable.

Remaining:
  1. Restart the session — the MCP server here still runs a pre-fix bundle, so resolve_claim is not exposed and captures produce ids without collision-safe suffixes.
  2. Exercise the public install path, removing the directory-source marketplace first because of the name collision (a2qj).
  3. Look at the GitHub-rendered README: the <picture> light/dark switch and the align=left float only reveal themselves once rendered.
  4. Answer q8w6 — whether Cowork actually works — by installing there, not by reasoning about it.
  5. Optional, the user's own file: a `Host github.com` block in ~/.ssh/config pinning the personal-account key (d29ka).

New commits remain safe (their CI bumps the SHA pin as commits land); only rewriting history would break it (c6vg).
