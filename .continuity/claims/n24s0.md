---
schema: 1
id: n24s0
type: next_action
title: >-
  Glama needs build-spec fields and a fresher commit, not a Dockerfile; then
  install + Cowork checks
status: superseded
confidence: confirmed
provenance:
  origin: auto
  created: '2026-09-03T04:19:50.128Z'
supersedes:
  - n23cr
superseded_by: n25xc
depends_on: []
tags: []
superseded_reason: >-
  The previous version told a future session to empty Glama's build steps. That
  would break the deployment: tsx is a devDependency, so `pnpm install` is what
  puts it on PATH for the CMD that runs it.
---

CORRECTED from a 2026-09-03 build log: Glama does NOT use the repository Dockerfile. Its pipeline builds its own debian:trixie-slim image with Node 24, mcp-proxy, pnpm and uv, then git clones the repo, checks out a pinned SHA, and runs the build steps from the BUILD SPEC. The repo Dockerfile is still valid for running the server standalone — do not delete it — it is simply not what Glama builds.

So the remaining Glama work is entirely in the admin UI at glama.ai/mcp/servers/vikcena01/ai-continuity-plugin/admin/dockerfile:
  - repoint the pinned commit; the successful build used c31c631, five commits stale, where glama.json does not yet exist so that check still fails
  - build steps: EMPTY. The default `pnpm install` resolved 110 packages for nothing, since dist/ is committed and fully bundled (d13)
  - command `node`, args `dist/mcp.js`
  - expect the coherence scan to hit 'No project selected' on a bare resume_context — that is rk5 behaving correctly; the working sequence is create_project, then record, then resume

Other open items unchanged: verify the public install path in a fresh session (remove the directory-source marketplace first, a2qj); answer q8w6 in the Cowork app; set the review baseline (97 claims, no marker).

Waiting: Claude submission in review; CI not scheduling (rk14so); OpenAI closed (x9nq).
