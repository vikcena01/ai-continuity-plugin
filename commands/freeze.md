---
description: Freeze a claim so it can never change (an unbreakable project invariant)
argument-hint: [claim-id]
allowed-tools: mcp__plugin_continuity_continuity__freeze_claim
---
Freeze the claim with id "$ARGUMENTS" using the `freeze_claim` tool, then confirm exactly what was frozen and why freezing means it won't be re-opened.
