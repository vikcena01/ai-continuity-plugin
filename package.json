{
  "name": "continuity",
  "version": "0.1.0",
  "description": "Portable, git-backed project-state for long-running AI work. Resume any AI session without losing decisions, constraints, or direction.",
  "type": "module",
  "bin": {
    "continuity": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "cli": "tsx src/cli.ts",
    "mcp": "tsx src/mcp.ts",
    "test": "npm run build && bash test/smoke.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.4.0",
    "gray-matter": "^4.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
